name: tabuqr
description: Aplikasi QRIS multifungsi — parse, generate, webhook, timeout.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  path_provider: ^2.1.2
  permission_handler: ^11.3.0
  qr_flutter: ^4.1.0
  image: ^4.1.3
  share_plus: ^7.2.1
  mobile_scanner: ^5.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
